---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  
  tasks:
    - name: Remove podman container if exists
      command: podman rm -f instance
      ignore_errors: true
      changed_when: false
      
    - name: Debug message
      debug:
        msg: "Container destroyed successfully"
